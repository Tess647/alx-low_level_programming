#include "main.h"

/**
  * print_rev - prints out strings in reverse order
  * @s: input parameter(pointer)
  * Return: Nothing
  */
void _puts(char *s)
{
	while (*str != '\0')
	{
		_putchar(str)
	}
}
